C_OBJECT:C1216($oCurrent)

$oCurrent:=WP_FillCurrent   // doc / paragraphe or picture

WP_SetTextIndent ($oCurrent;Self:C308->;wk unit pt:K81:136)
  // do the translation into  usr unit for input area
WP_GetTextIndent ($oCurrent)